'use client';

/**
 * Stub — chat feature is WIP. Committed pages call useChatUiOptional();
 * return null so the Chat button stays hidden until the full chat tree lands.
 */
export function useChatUiOptional(): null {
  return null;
}
