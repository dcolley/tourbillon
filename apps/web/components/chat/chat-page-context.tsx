'use client';

/**
 * Stub — chat feature is WIP. Pages render this as a no-op component
 * so they compile before the full chat system lands.
 */
export function ChatPageContext(_props: Record<string, unknown>) {
  return null;
}

export type ChatOpenTarget = { agentId: string; resourceId?: string };
